# scheduler.yml has been REPLACED by direct GitHub Actions cron workflows.
#
# The original scheduler.yml tried to call an external API endpoint
# (secrets.API_BASE_URL/internal/predict) which does not exist.
# This caused silent failures on every 4-hour cron run.
#
# REPLACEMENT: All scheduling is now handled by:
#   - predict_workflow.yml  → runs every 6 hours (generates predictions)
#   - verify_workflow.yml   → runs daily at 2 AM UTC (verifies outcomes)
#   - train_workflow.yml    → runs every Sunday (retrains champion model)
#   - daily_update.yml      → runs daily at 6 AM UTC (data sync)
#
# DO NOT RE-ENABLE this file.
# name: Scheduler (DISABLED - replaced by individual workflow files)
# on: {}
